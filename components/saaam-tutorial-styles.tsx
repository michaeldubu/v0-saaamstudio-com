"use client"

import { createGlobalStyle } from "styled-components"

export const SaaamTutorialStyles = createGlobalStyle`
  @media (max-width: 640px) {
    .saaam-tutorial-code {
      font-size: 0.8rem;
      line-height: 1.4;
      white-space: pre-wrap;
      word-break: break-word;
    }
  }
`
